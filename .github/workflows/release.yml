name: Build and Release Extension

on:
  push:
    branches:
      - main
    paths:
      - 'manifest.json'
  workflow_dispatch: {}

permissions:
  contents: write

jobs:
  check-version:
    runs-on: ubuntu-latest
    outputs:
      new_version: ${{ steps.version_check.outputs.new_version }}
      previous_version: ${{ steps.version_check.outputs.previous_version }}
      release_exists: ${{ steps.check_release.outputs.release_exists }}
    steps:
      - name: Checkout code
        uses: actions/checkout@v4
        with:
          fetch-depth: 2

      - name: Get compare versions from manifest.json and previous release
        id: version_check
        run: |
          # Get current version from manifest.json
          CURR_VERSION=$(jq -r '.version' manifest.json)
          echo "Current version: $CURR_VERSION"
          echo "new_version=$CURR_VERSION" >> $GITHUB_OUTPUT

          # Get previous version from the previous release
          PREV_VERSION=$(gh release view --json tagName --jq '.tagName' v$CURR_VERSION)
          echo "Previous version: $PREV_VERSION"
          echo "previous_version=$PREV_VERSION" >> $GITHUB_OUTPUT

      - name: Check if release already exists
        id: check_release
        run: |
          VERSION="${{ steps.version_check.outputs.new_version }}"
          if gh release view "v$VERSION" >/dev/null 2>&1; then
            echo "Release v$VERSION already exists"
            echo "release_exists=true" >> $GITHUB_OUTPUT
          else
            echo "Release v$VERSION does not exist"
            echo "release_exists=false" >> $GITHUB_OUTPUT
          fi
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

  build-and-release:
    needs: check-version
    if: needs.check-version.outputs.release_exists == 'false'
    runs-on: ubuntu-latest
    steps:
      - name: Checkout code
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '22'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Build extension
        run: |
          echo "${{ secrets.EXTENSION_KEY }}" > extension-key.pem
          ls -la .
          npm run build
      
      - name: Generate changelog
        id: changelog
        run: |
          VERSION="${{ needs.check-version.outputs.new_version }}"
          PREV_VERSION="${{ needs.check-version.outputs.previous_version }}"
          echo "## Changes in v$VERSION" > CHANGELOG.md
          echo "" >> CHANGELOG.md
          echo "### Commits since v$PREV_VERSION:" >> CHANGELOG.md
          git log --oneline "v$PREV_VERSION"..HEAD --grep="^(?!Merge)" >> CHANGELOG.md 2>/dev/null || true
          echo "" >> CHANGELOG.md
          echo "### Installation" >> CHANGELOG.md
          echo "- Download the \`reNorsk-v$VERSION.zip\` file for manual installation" >> CHANGELOG.md
          echo "- Or download the \`reNorsk-v$VERSION.crx\` file for direct installation (may require developer mode)" >> CHANGELOG.md

      - name: Create GitHub Release
        id: create_release
        uses: softprops/action-gh-release@v2
        with:
          tag_name: "v${{ needs.check-version.outputs.new_version }}"
          name: "reNorsk v${{ needs.check-version.outputs.new_version }}"
          body_path: "CHANGELOG.md"
          draft: false
          prerelease: false
          files: |
            ChromeExtension/v3/reNorsk.zip
            ChromeExtension/v3/reNorsk.crx
        env:
          GITHUB_TOKEN: ${{ secrets.GITHUB_TOKEN }}

      - name: Summary
        run: |
          VERSION="${{ needs.check-version.outputs.new_version }}"
          echo "🎉 Successfully created release v$VERSION"
          echo "📦 Assets created:"
          ls -la ChromeExtension/v3/
          echo ""
          echo "🔗 Release URL: ${{ steps.create_release.outputs.html_url }}"
